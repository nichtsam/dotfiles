- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc
    ~/.ssh/config:
      path: ssh/config
      create: true

- shell:
    - [git submodule update --init --recursive, Installing submodules]
